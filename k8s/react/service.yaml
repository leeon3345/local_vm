apiVersion: v1
kind: Service
metadata:
  name: react-svc
  namespace: default
  labels:
    app: react
spec:
  selector:
    app: react
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
      nodePort: 30080  # 30000-32767 범위